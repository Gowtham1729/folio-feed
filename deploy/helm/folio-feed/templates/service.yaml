apiVersion: v1
kind: Service
metadata:
  name: {{ include "folio-feed.fullname" . }}
  labels:
    {{- include "folio-feed.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "folio-feed.selectorLabels" . | nindent 4 }}
