apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "folio-feed.fullname" . }}-test-connection"
  labels:
    {{- include "folio-feed.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "folio-feed.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
